@require(table_obj)
[
@`',\n'.join(['    {\n    %s\n    }' % ',\n    '.join([f'    "{field_name}" : {format_value(value)}' for field_name, value in zip(table_obj.field_names, row)]) for row in table_obj.row_iter])`
]
